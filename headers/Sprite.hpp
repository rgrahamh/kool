class Sprite {
	private:
			std::vector<String> images; //Store file paths to each image. We need a way to store images in memory for easy access
	public:
			int xOffset;
			int yOffset;
			int animationSpeed; //Float instead maybe? IDK

}
